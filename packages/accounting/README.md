# @erpax/accounting

erpax **accounting** — sequence **6 (hooks)**: the universal ledger — double-entry journals, GL, polymorphic accountability.

Built from [`src/accounting`](https://github.com/erpax/erpax/tree/main/src/accounting) by `packages/build.mjs`; the runtime closure is bundled, bare imports stay external (computed into dependencies), and the closure ratchet (`erpax.closureCeiling`) fails the build closed if entanglement grows.

Licence: CC-BY-NC-ND-4.0 (or commercial via license@erpax.com). Free core math lives in `@erpax/algebra` (MIT).
