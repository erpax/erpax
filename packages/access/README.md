# @erpax/access

erpax **access** — sequence **3·9 (control)**: RBAC, multi-tenancy, auth — the control plane.

Built from [`src/access`](https://github.com/erpax/erpax/tree/main/src/access) by `packages/build.mjs`; the runtime closure is bundled, bare imports stay external (computed into dependencies), and the closure ratchet (`erpax.closureCeiling`) fails the build closed if entanglement grows.

Licence: CC-BY-NC-ND-4.0 (or commercial via license@erpax.com). Free core math lives in `@erpax/algebra` (MIT).
