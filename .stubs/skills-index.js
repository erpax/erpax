// PROD CLIENT STUB — never ship skills.index (≈77MB) into the browser/Worker.
// Agents use realiseSkillsForPath / loadSkillByAtomPath (lazy disk), not this barrel.
export const skillsIndex = []
export default []
