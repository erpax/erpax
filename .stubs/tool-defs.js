// PROD-BUILD STUB for src/agents/mcp/tool-defs.ts — the erpax corpus MCP tool surface is a
// dev/agent facet; in prod the in-process client carries no corpus tools (agents use the
// official /api/mcp gateway). Server build only. buildErpaxMcpTools is the sole consumed value.
export const buildErpaxMcpTools = () => []
