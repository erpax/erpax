// PROD-BUILD STUB for asset imports (.svg, .png, .scss) — the server Worker never renders
// UI components so these assets are pure dead weight. Stub returns empty string for asset
// imports so esbuild won't fail on unknown file types.
export default ''
