import type { Payload } from 'payload'

/**
 * Seed for `memories` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedMemories(_payload: Payload): Promise<void> {
  // Opening data for `memories` is seeded here (none by default).
}
