/** contact — barrel; the atom's one face. @see ./SKILL.md */
export * from './form'
export * from './page'

/** @index-cross.foldback child=seed/contact parent=seed — this cross folds back into its parent. */
