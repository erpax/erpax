---
name: contact
description: "Use for the contact seed pair — the form definition and the page that renders it, seeded together because a page without its form is a dead route."
atomPath: "seed/contact"
---
# contact

The contact seed. `form` is the form definition and `page` the page that renders it; they seed together because a page referencing a form that was never seeded is a dead route.

Composes: [[seed]] · [[law]].
