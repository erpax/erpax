/**
 * confirm — the write-time seal, as a barrel over its four lanes.
 *
 * A hub holds no matter of its own ([[rules]]/concentration): each lane is the child
 * atom it already was, and this file is only the face they are reached through.
 *
 *   uuid    the substrate-independent gate stack — `pnpm confirm:uuid`
 *   matter  the payload ⊗ vitepress ⊗ build lane, scoped or `--full`
 *   probe   the compile probe gateTypecheck loads (never imports this barrel)
 *   push    the stop-hook body: seal → commit → push, detached
 *
 * @see ./SKILL.md — ./uuid — ./matter — ./probe — ./push
 */
export {
  IMPORT_PURITY_BASELINE,
  DIAMOND_LAW_ATOMS,
  gateAura,
  gateFolders,
  gateImports,
  gateTypecheck,
  gateReadme,
  gateBoundary,
  gateDiamond,
  gateTypography,
  gateCloudflareAi,
  uuidGates,
  uuidConfirm,
} from './uuid'
export type { UuidGateResult, UuidConfirmResult } from './uuid'

export {
  OUTSIDE_ALLOW,
  CONFIRM_CHECK_AXES,
  BUILD_GATE_CHECKS,
  outsideMatter,
  touchesStandardBanner,
  folderNameWarnings,
  vitepressConfirm,
  payloadConfirm,
  scopeFiles,
  fullConfirm,
} from './matter'
export type { VitepressConfirmResult, PayloadConfirmResult } from './matter'

export * from './probe'
