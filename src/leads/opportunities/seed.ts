import type { Payload } from 'payload'

/**
 * Seed for `opportunities` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedOpportunities(_payload: Payload): Promise<void> {
  // Opening data for `opportunities` is seeded here (none by default).
}
