import type { Payload } from 'payload'

/**
 * Seed for `carriers` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCarriers(_payload: Payload): Promise<void> {
  // Opening data for `carriers` is seeded here (none by default).
}
