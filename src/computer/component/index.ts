/**
 * computer/component — renders atoms on screen; pivot to canonical @/component.
 * ONE node, zero duplication ([[merge]] at path scale).
 */
export { componentPixel, sameComponent } from '@/component'

/** @index-cross.foldback child=computer/component parent=computer — this cross folds back into its parent. */
