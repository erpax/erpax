/**
 * computer/network — connects externally; vocabulary pivot to @/network.
 */
export const PART = 'network' as const
export const CANONICAL = 'network' as const
export const PARENT = 'computer' as const
export const atomPath = 'computer/network' as const

/** @index-cross.foldback child=computer/network parent=computer — this cross folds back into its parent. */
