/** agent/team — barrel so `@/agent/team` is an index import (basicTeams without bootstrap cycle). */
export {
  BASIC_TEAM_SIZES,
  TEAM_DISCIPLINE,
  basicTeams,
  formTeam,
  isBasicTeam,
  mergeTeams,
  shareSkills,
  spawnTeam,
  teamCovers,
  teamGrouping,
  teamSkills,
  teamUuid,
  type BasicTeamSize,
  type Team,
  type TeamCoverage,
} from '../team'
