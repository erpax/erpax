/** standards — barrel; the atom's one face. @see ./SKILL.md */
export * from './import'
export * from './service'

/** @index-cross.foldback child=export/standards parent=export — this cross folds back into its parent. */
