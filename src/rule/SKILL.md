---
name: rule
description: "Use when modelling one rule — the singular model of the rules collection (the plural store); a condition-action statement the system enforces."
---

# rule — the model of one [[rules]] row

A condition-action statement the system enforces. The singular model whose plural store is the [[rules]] collection ([[balance]]: every collection has its model).

Composes [[rules]] · [[governance]] · [[balance]].
