/** link/component — client CMSLink facet (kept off `@/link` barrel for typegen). */
export { CMSLink } from '../cms'

/** @index-cross.foldback child=link/component parent=link — this cross folds back into its parent. */
