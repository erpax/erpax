import type { Payload } from 'payload'

/**
 * Seed for `time-entries` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTimeEntries(_payload: Payload): Promise<void> {
  // Opening data for `time-entries` is seeded here (none by default).
}
