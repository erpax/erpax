import type { Payload } from 'payload'

/**
 * Seed for `share-based-payments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedShareBasedPayments(_payload: Payload): Promise<void> {
  // Opening data for `share-based-payments` is seeded here (none by default).
}
