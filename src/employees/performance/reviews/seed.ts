import type { Payload } from 'payload'

/**
 * Seed for `performance-reviews` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPerformanceReviews(_payload: Payload): Promise<void> {
  // Opening data for `performance-reviews` is seeded here (none by default).
}
