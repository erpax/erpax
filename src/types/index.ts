/**
 * types — Payload generated types barrel. Re-exports the generated `payload-types.ts`
 * so consumers entangle at `@/types` (the atom index), never the deep generated file.
 *
 * @see ./SKILL.md — regenerate via `payload generate:types` after schema changes
 */
export type * from '../payload-types'
export * from '../payload-types'
