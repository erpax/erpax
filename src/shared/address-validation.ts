/**
 * @deprecated DO NOT IMPORT FROM HERE. The canonical address validator
 * dissolved to `@/address/validation` (the `validateAddress` factory)
 * and its Payload `beforeValidate` wrapper to `@/validate/address`
 * (`validateAddressHook`). This file is a relocation record only — the
 * `collections/` grouping prefix and the "shared" sub-bucket were
 * dropped in the single-word-matrix dissolution.
 *
 * @audit ISO-19011:2018 audit-trail relocation-record
 * @see @/address/validation
 * @see @/validate/address
 */

export {}
