/**
 * @deprecated DO NOT IMPORT FROM HERE. The common/shared field shapes
 * dissolved to the `@/shared` atom — reusable Payload `Field` factories
 * live in `@/shared/field` (re-exported from `@/shared`), render atoms
 * in `@/shared/{Money,AuditedTimestamp,AddressBlock}`. This file is a
 * relocation record only — the `collections/` grouping prefix and the
 * "shared" sub-bucket were dropped in the single-word-matrix dissolution.
 *
 * @audit ISO-19011:2018 audit-trail relocation-record
 * @see @/shared
 * @see @/shared/field
 */

export {}
