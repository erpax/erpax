---
name: vocabulary
description: "Use when checking that every word in the corpus comes from the shared standards-grounded vocabulary — the dictionary, schema.org, standard codes, and established domain terms. Whatever the language, words are written only from this one scheme; an ungrounded word is entropy, and the audit computes its zero-entropy solution."
atomPath: vocabulary
coordinate: vocabulary · 1/base · 2ec0c3ee
contentUuid: "a2c0e7ba-0753-511e-b70e-9ed7fc22ea33"
diamondUuid: "69c93502-7288-83c4-90c2-4112511a6845"
uuid: "2ec0c3ee-0045-8b90-a5a1-37a4551f277d"
horo: 1
bonds:
  in:
    - atom
    - collapse
    - concatenate
    - entropy
    - matrix
    - merge
    - standards
    - tamper
    - trinity
    - unavoidable
    - vocabulary
    - word
  out:
    - atom
    - collapse
    - concatenate
    - entropy
    - matrix
    - merge
    - standards
    - tamper
    - trinity
    - unavoidable
    - vocabulary
    - word
typography:
  partition: vocabulary
  bondDegree: 44
  neighbors: []
standards:
  - "ISO 25964 / SKOS — a controlled vocabulary: terms from one shared scheme"
  - "computed over the live uuid-matrix atoms against ./words.ts; never hand-asserted"
bindings: []
neighbors:
  wikilink:
    - atom
    - collapse
    - entropy
    - matrix
    - merge
    - standards
    - tamper
    - trinity
    - unavoidable
    - word
  matrix:
    - atom
    - collapse
    - concatenate
    - entropy
    - matrix
    - merge
    - standards
    - tamper
    - trinity
    - unavoidable
    - vocabulary
    - word
  backlinks:
    - atom
    - collapse
    - concatenate
    - entropy
    - matrix
    - merge
    - standards
    - tamper
    - trinity
    - unavoidable
    - vocabulary
    - word
signatures:
  computationUuid: "e76908ca-800c-80fc-8d9b-244ded8f985d"
  stages:
    - stage: path
      stageUuid: "f0b18baa-02eb-8261-9d44-76631e7d803a"
    - stage: trinity
      stageUuid: "b8d89f6a-cf5e-8f71-a5ae-59d02c09d946"
    - stage: boundary
      stageUuid: "e5c9a21a-a41f-8a3e-8a82-133e53e37b0c"
    - stage: links
      stageUuid: "10d0c2b3-2ee1-87ff-aaca-e0b41f10aeaa"
    - stage: horo
      stageUuid: "c587656c-6bfe-800d-8a8d-18db2106abac"
    - stage: seal
      stageUuid: "7f01fe90-e934-8219-b5fc-92ea056994e8"
    - stage: uuid
      stageUuid: "c337e4c9-8980-813b-87bc-c4d49745921c"
version: 2
---
# vocabulary — words only from the shared scheme

Whatever the language, the WORDS are written only from one shared vocabulary. The scheme (`./words.ts`, generated by `scripts/vocab-gen.mjs`) is grounded in standards: the English dictionary (∪ inflections), **schema.org** (the `@schema.org component word` atoms — the standard carries even its own spellings, so `clincal`/`biomechnical` are grounded, not typos), **standard codes** (numeric ISO/EN versions, abbreviations), and a curated set of established **domain** terms. An [[atom]] is GROUNDED iff it is a vocabulary root or COMPOSES from roots (`budgetvariance` → `budget` + `variance`).

An ungrounded word is [[entropy]] — and a typo, an ad-hoc invention, and a forgery are the same thing: a word from outside the scheme. So the gate is a [[tamper]]-cost lever ([[unavoidable]]): every word wired to the shared vocabulary is one more dimension a forger must satisfy. `audit()` drives that entropy to ZERO; the corpus is 0-entropy today (all 2241 atoms grounded).

**The errors compute the solution.** When a stray slips in, `./test.ts` does not merely flag it — `suggest()` computes the zero-entropy fix: the nearest vocabulary word by edit distance (a typo is one edit from the scheme), or the decomposition into roots (`path=type`), or "extend the vocabulary." The fix that restores 0 entropy is read straight off the failure.

This is the [[merge]] law at the word level (same word = same root = same uuid) and the sibling of md-purity ([[trinity]]) and the [[standards]] scheme. New words must extend `words.ts` deliberately — never invented in place.

Matter-twin: `src/vocabulary/index.ts`. Composes [[entropy]] · [[tamper]] · [[unavoidable]] · [[merge]] · [[trinity]] · [[standards]] · [[word]] · [[collapse]] · [[matrix]].

## Standards
- ISO 25964 / W3C SKOS — a controlled vocabulary (terms from one shared concept scheme)
