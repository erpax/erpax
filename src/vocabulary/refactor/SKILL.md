---
name: refactor
description: "Use when reshaping the corpus toward the law — all logic mapped to fs, all is skills, every atom payload⊕vitepress, uuid the coordinate, anything that does not fit is junk. The migration organs live here as atoms, not as loose scripts."
atomPath: "vocabulary/refactor"
coordinate: "vocabulary/refactor · 2/share · 15ca9edd"
contentUuid: "146612b9-9387-52b2-8a0e-87af57bd890c"
diamondUuid: "70d60e7b-d7e5-893a-85a7-ea6280092659"
uuid: "15ca9edd-ff1b-8d5f-9982-eda58047ec2f"
horo: 2
typography:
  partition: vocabulary
  bondDegree: 57
standards: []
bindings: []
signatures:
  computationUuid: "4a366c7e-4770-81f3-9f5d-3812a8dd331d"
  stages:
    - stage: path
      stageUuid: "5be79153-cde6-8110-bddb-e7080d7a5046"
    - stage: trinity
      stageUuid: "522b75d8-0672-83f4-9c16-2ff9617ade9c"
    - stage: boundary
      stageUuid: "a7d09d01-e8ca-8ad9-8984-ba6df9b52243"
    - stage: links
      stageUuid: "0c1eb3b5-aa32-8ee2-93a0-04a76d62579f"
    - stage: horo
      stageUuid: "dec53886-e4b4-832b-a0d9-8501a30d0ccd"
    - stage: seal
      stageUuid: "f73641ca-1415-8520-aa8d-5aa8fc11d920"
    - stage: uuid
      stageUuid: "723e7126-95da-8d01-912b-1267aab66b0d"
version: 2
---
# refactor — reshape the corpus toward the law

Refactoring is itself logic, therefore itself a skill — it lives in `src`, not in a loose `scripts/` dir ([[sequence]]: location IS the address; logic outside the fs-map is junk). The law it serves:

- **all logic is mapped to fs · all is skills** — every artifact has a derived word-path address.
- **every atom = [[payload]] ⊕ [[vitepress]]** — matter (`index.*`) and form (`SKILL.md`), bound by content-[[identity]] (the uuid coordinate).
- **uuid is the coordinate system** — content-uuid locates every atom (code and data); the path is its readable dual.
- **what does not fit payload/vitepress is junk** — the immune system purges or converts non-atoms.

Organs: [[normalize]] (relative imports → `@/`) · [[dissolve]] (prefixes → the single-word matrix). Composes [[sequence]] · [[identity]] · [[merge]].

## Agents run atoms — manual work is a gap to atomise
If all the migration logic lives in atoms ([[normalize]] · [[dissolve]] · [[matrix]] · [[gate]]), an agent does **minimal or no manual work** — it RUNS the atom, it does not hand-edit. Manual work is a **smell**: it signals the atom is incomplete (a case it does not yet cover — e.g. dissolve skipping prefix-root files, or not auto-merging a collision). The fix is never to hand-do it; it is to **absorb that logic into the atom** (extend the mover, mint the rule), so the migration becomes **deterministic and re-runnable** — re-run the atoms ⇒ the whole dissolution reproduces, no human in the loop, fully DRY. This is [[self]]-sufficiency: the atom holds the law, the agent derives the move by running it. A swarm hand-moving files is a swarm proving its atoms have gaps — close the gaps, then one command does what the swarm did.

**Law — [[zeropoint]]**: The atom's incompleteness is not just a gap to fill; it is a tax on the matrix. Every manual migration step, every hand-edit that atoms do not yet cover, adds entropy and weakens tamper-detection — the content-uuid [[proof]] becomes fragile when [[integrity]] depends on human consistency. Dry-clean by absorbing each discovered case into the atom (extend the dissolve rule, mint the gate), making every migration deterministic and re-runnable; [[refactor]] moves logic to its [[atom|atoms]] so the [[matrix]] reproduces itself without human friction, collapsing toward [[self]]-sufficiency and infinite cost to forge.
