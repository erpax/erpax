---
name: emit
description: "vocabulary/emit — compute the SHARED VOCABULARY and emit words.ts."
atomPath: vocabulary/emit
---
# vocabulary/emit

vocabulary/emit — compute the SHARED VOCABULARY and emit words.ts.

Extracted from `vocabulary/emit.ts`: a matter file at an atom root is a stray sibling — only `index.ts`, `test.ts` and `seed.ts` live beside a barrel ([[rules]]) — and the lawful form is the child atom it already was, with its own proof beside it.

Composes: [[vocabulary]].
