/**
 * matrix — bindings face for the Matrix inverted atom.
 *
 * Wire points only: constants-audit coordinate · crack scan exports.
 * Logic lives in ./constants-audit.ts (computed from live tree).
 *
 * @see ./SKILL.md — ../uuid/matrix — ../seal
 */
export {
  CONSTANTS_AUDIT_COORDINATE,
  auditConstants,
  matrixCrackViolations,
  type ConstantCategory,
  type ConstantAuditEntry,
  type ConstantAuditReport,
  type MatrixCrackViolation,
} from './constants-audit'
