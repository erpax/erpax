import type { Payload } from 'payload'

/**
 * Seed for `kyc-checks` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedKycChecks(_payload: Payload): Promise<void> {
  // Opening data for `kyc-checks` is seeded here (none by default).
}
