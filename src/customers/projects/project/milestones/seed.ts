import type { Payload } from 'payload'

/**
 * Seed for `project-milestones` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedProjectMilestones(_payload: Payload): Promise<void> {
  // Opening data for `project-milestones` is seeded here (none by default).
}
