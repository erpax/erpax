import type { Payload } from 'payload'

/**
 * Seed for `project-tasks` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedProjectTasks(_payload: Payload): Promise<void> {
  // Opening data for `project-tasks` is seeded here (none by default).
}
