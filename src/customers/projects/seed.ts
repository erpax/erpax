import type { Payload } from 'payload'

/**
 * Seed for `projects` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedProjects(_payload: Payload): Promise<void> {
  // Opening data for `projects` is seeded here (none by default).
}
