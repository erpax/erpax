import type { Payload } from 'payload'

/**
 * Seed for `returns` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedReturns(_payload: Payload): Promise<void> {
  // Opening data for `returns` is seeded here (none by default).
}
