import type { Payload } from 'payload'

/**
 * Seed for `sales-orders` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedSalesOrders(_payload: Payload): Promise<void> {
  // Opening data for `sales-orders` is seeded here (none by default).
}
