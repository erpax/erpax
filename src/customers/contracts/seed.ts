import type { Payload } from 'payload'

/**
 * Seed for `contracts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedContracts(_payload: Payload): Promise<void> {
  // Opening data for `contracts` is seeded here (none by default).
}
