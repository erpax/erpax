import type { Payload } from 'payload'

/**
 * Seed for `packages` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPackages(_payload: Payload): Promise<void> {
  // Opening data for `packages` is seeded here (none by default).
}
