export { inventoryMovementPostingHook } from './movement'

/** @index-cross.foldback child=items/inventory/movements/hooks parent=items/inventory/movements — this cross folds back into its parent. */
