import type { Payload } from 'payload'

/**
 * Seed for `production-receipts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedProductionReceipts(_payload: Payload): Promise<void> {
  // Opening data for `production-receipts` is seeded here (none by default).
}
