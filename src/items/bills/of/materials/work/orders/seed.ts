import type { Payload } from 'payload'

/**
 * Seed for `work-orders` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedWorkOrders(_payload: Payload): Promise<void> {
  // Opening data for `work-orders` is seeded here (none by default).
}
