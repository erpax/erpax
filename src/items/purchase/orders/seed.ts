import type { Payload } from 'payload'

/**
 * Seed for `purchase-orders` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPurchaseOrders(_payload: Payload): Promise<void> {
  // Opening data for `purchase-orders` is seeded here (none by default).
}
