import type { Payload } from 'payload'

/**
 * Seed for `recruiting-pipeline` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRecruitingPipeline(_payload: Payload): Promise<void> {
  // Opening data for `recruiting-pipeline` is seeded here (none by default).
}
