import type { Payload } from 'payload'

/**
 * Seed for `reporting-standards` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedReportingStandards(_payload: Payload): Promise<void> {
  // Opening data for `reporting-standards` is seeded here (none by default).
}
