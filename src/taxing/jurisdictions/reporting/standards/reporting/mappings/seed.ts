import type { Payload } from 'payload'

/**
 * Seed for `reporting-mappings` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedReportingMappings(_payload: Payload): Promise<void> {
  // Opening data for `reporting-mappings` is seeded here (none by default).
}
