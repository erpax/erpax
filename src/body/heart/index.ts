/**
 * body/heart — organ facet; pivot to canonical @/heart.
 * ONE node, zero duplication ([[merge]] at path scale).
 */
export * from '@/heart'

/** @index-cross.foldback child=body/heart parent=body — this cross folds back into its parent. */
