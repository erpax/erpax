/**
 * body/artery — organ facet; pivot to canonical @/artery.
 * ONE node, zero duplication ([[merge]] at path scale).
 */
export * from '@/artery'

/** @index-cross.foldback child=body/artery parent=body — this cross folds back into its parent. */
