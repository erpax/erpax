/**
 * body/leg — anatomical part; vocabulary pivot to @/leg.
 */
export const PART = 'leg' as const
export const CANONICAL = 'leg' as const
export const PARENT = 'body' as const
export const atomPath = 'body/leg' as const

/** @index-cross.foldback child=body/leg parent=body — this cross folds back into its parent. */
