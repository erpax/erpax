/**
 * body/nerve — organ facet; pivot to canonical @/nerve.
 * ONE node, zero duplication ([[merge]] at path scale).
 */
export * from '@/nerve'

/** @index-cross.foldback child=body/nerve parent=body — this cross folds back into its parent. */
