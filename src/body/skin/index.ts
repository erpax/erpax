/**
 * body/skin — organ facet; pivot to canonical @/skin.
 * ONE node, zero duplication ([[merge]] at path scale).
 */
export * from '@/skin'

/** @index-cross.foldback child=body/skin parent=body — this cross folds back into its parent. */
