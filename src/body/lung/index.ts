/**
 * body/lung — organ facet; pivot to canonical @/lung.
 * ONE node, zero duplication ([[merge]] at path scale).
 */
export * from '@/lung'

/** @index-cross.foldback child=body/lung parent=body — this cross folds back into its parent. */
