/**
 * body/vein — organ facet; pivot to canonical @/vein.
 * ONE node, zero duplication ([[merge]] at path scale).
 */
export * from '@/vein'

/** @index-cross.foldback child=body/vein parent=body — this cross folds back into its parent. */
