/**
 * body/blood — organ facet; pivot to canonical @/blood.
 * ONE node, zero duplication ([[merge]] at path scale).
 */
export * from '@/blood'

/** @index-cross.foldback child=body/blood parent=body — this cross folds back into its parent. */
