/**
 * body/brain — organ facet; pivot to canonical @/brain.
 * ONE node, zero duplication ([[merge]] at path scale).
 */
export * from '@/brain'

/** @index-cross.foldback child=body/brain parent=body — this cross folds back into its parent. */
