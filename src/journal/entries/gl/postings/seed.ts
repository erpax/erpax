import type { Payload } from 'payload'

/**
 * Seed for `gl-postings` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedGlPostings(_payload: Payload): Promise<void> {
  // Opening data for `gl-postings` is seeded here (none by default).
}
