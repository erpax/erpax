import type { Payload } from 'payload'

/**
 * Seed for `bookable-resources` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBookableResources(_payload: Payload): Promise<void> {
  // Opening data for `bookable-resources` is seeded here (none by default).
}
