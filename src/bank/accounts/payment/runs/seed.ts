import type { Payload } from 'payload'

/**
 * Seed for `payment-runs` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPaymentRuns(_payload: Payload): Promise<void> {
  // Opening data for `payment-runs` is seeded here (none by default).
}
