import type { Payload } from 'payload'

/**
 * Seed for `regulatory-deferral-accounts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRegulatoryDeferralAccounts(_payload: Payload): Promise<void> {
  // Opening data for `regulatory-deferral-accounts` is seeded here (none by default).
}
