import type { Payload } from 'payload'

/**
 * Seed for `sepa-mandates` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedSepaMandates(_payload: Payload): Promise<void> {
  // Opening data for `sepa-mandates` is seeded here (none by default).
}
