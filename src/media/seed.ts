import type { Payload } from 'payload'

/**
 * Seed for `media` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedMedia(_payload: Payload): Promise<void> {
  // Opening data for `media` is seeded here (none by default).
}
