/**
 * blocks/form — the barrel: the field registry, the block component, and the shared wrapper.
 *
 * @see ./SKILL.md
 */
export { fields } from './fields'
export { FormField, type FormFieldProps } from './FormField'
export { FormBlock, type FormBlockType } from './Component'

/** @index-cross.foldback child=blocks/form parent=blocks — this cross folds back into its parent. */
