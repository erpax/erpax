import{describe,it,expect}from'vitest'
import * as m from '@/users/hooks'
describe('users/hooks',()=>{it('the atom exports real matter — the stub test asserts existence, the SKILL states the law',()=>{expect(Object.keys(m).length).toBeGreaterThan(0)})})
