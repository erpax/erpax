import type { Payload } from 'payload'

/**
 * Seed for `audit-samples` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAuditSamples(_payload: Payload): Promise<void> {
  // Opening data for `audit-samples` is seeded here (none by default).
}
