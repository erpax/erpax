import type { Payload } from 'payload'

/**
 * Seed for `remediation-plans` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRemediationPlans(_payload: Payload): Promise<void> {
  // Opening data for `remediation-plans` is seeded here (none by default).
}
