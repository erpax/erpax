/** legal — barrel; the atom's one face. @see ./SKILL.md */
export * from './agent'
export * from './conflict'

/** @index-cross.foldback child=agents/registered/legal parent=agents/registered — this cross folds back into its parent. */
