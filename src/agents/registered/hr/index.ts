/** hr — barrel; the atom's one face. @see ./SKILL.md */
export * from './agent'
export * from './training'

/** @index-cross.foldback child=agents/registered/hr parent=agents/registered — this cross folds back into its parent. */
