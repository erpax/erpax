import type { Payload } from 'payload'

/**
 * Seed for `regulatory-reports` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRegulatoryReports(_payload: Payload): Promise<void> {
  // Opening data for `regulatory-reports` is seeded here (none by default).
}
