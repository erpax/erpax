import type { Payload } from 'payload'

/**
 * Seed for `audit-committee-minutes` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAuditCommitteeMinutes(_payload: Payload): Promise<void> {
  // Opening data for `audit-committee-minutes` is seeded here (none by default).
}
