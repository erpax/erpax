import type { Payload } from 'payload'

/**
 * Seed for `related-party-transactions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRelatedPartyTransactions(_payload: Payload): Promise<void> {
  // Opening data for `related-party-transactions` is seeded here (none by default).
}
