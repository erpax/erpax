import type { Payload } from 'payload'

/**
 * Seed for `consolidations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedConsolidations(_payload: Payload): Promise<void> {
  // Opening data for `consolidations` is seeded here (none by default).
}
