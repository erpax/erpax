import type { Payload } from 'payload'

/**
 * Seed for `fiscal-calendars` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedFiscalCalendars(_payload: Payload): Promise<void> {
  // Opening data for `fiscal-calendars` is seeded here (none by default).
}
