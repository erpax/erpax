import type { Payload } from 'payload'

/**
 * Seed for `business-combinations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBusinessCombinations(_payload: Payload): Promise<void> {
  // Opening data for `business-combinations` is seeded here (none by default).
}
