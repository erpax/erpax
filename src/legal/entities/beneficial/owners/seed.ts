import type { Payload } from 'payload'

/**
 * Seed for `beneficial-owners` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBeneficialOwners(_payload: Payload): Promise<void> {
  // Opening data for `beneficial-owners` is seeded here (none by default).
}
