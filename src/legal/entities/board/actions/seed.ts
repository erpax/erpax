import type { Payload } from 'payload'

/**
 * Seed for `board-actions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBoardActions(_payload: Payload): Promise<void> {
  // Opening data for `board-actions` is seeded here (none by default).
}
