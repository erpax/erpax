import type { Payload } from 'payload'

/**
 * Seed for `segment-reporting` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedSegmentReporting(_payload: Payload): Promise<void> {
  // Opening data for `segment-reporting` is seeded here (none by default).
}
