import type { Payload } from 'payload'

/**
 * Seed for `closing-entries` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedClosingEntries(_payload: Payload): Promise<void> {
  // Opening data for `closing-entries` is seeded here (none by default).
}
