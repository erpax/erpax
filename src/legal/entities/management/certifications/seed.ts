import type { Payload } from 'payload'

/**
 * Seed for `management-certifications` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedManagementCertifications(_payload: Payload): Promise<void> {
  // Opening data for `management-certifications` is seeded here (none by default).
}
