import type { Payload } from 'payload'

/**
 * Seed for `gl-accounts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedGlAccounts(_payload: Payload): Promise<void> {
  // Opening data for `gl-accounts` is seeded here (none by default).
}
