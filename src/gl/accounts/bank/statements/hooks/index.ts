export { bankStatementImportedHook } from './statement'

/** @index-cross.foldback child=gl/accounts/bank/statements/hooks parent=gl/accounts/bank/statements — this cross folds back into its parent. */
