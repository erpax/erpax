import type { Payload } from 'payload'

/**
 * Seed for `bank-statements` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBankStatements(_payload: Payload): Promise<void> {
  // Opening data for `bank-statements` is seeded here (none by default).
}
