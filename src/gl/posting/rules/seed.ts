import type { Payload } from 'payload'

/**
 * Seed for `gl-posting-rules` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedGlPostingRules(_payload: Payload): Promise<void> {
  // Opening data for `gl-posting-rules` is seeded here (none by default).
}
