import type { Payload } from 'payload'

/**
 * Seed for `consolidation-eliminations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedConsolidationEliminations(_payload: Payload): Promise<void> {
  // Opening data for `consolidation-eliminations` is seeded here (none by default).
}
