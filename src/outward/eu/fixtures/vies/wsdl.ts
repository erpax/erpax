/**
 * VIES VAT-number checkService WSDL, frozen.
 *
 * A frozen CAPTURE, byte-for-byte, carried as a module rather than a loose `.xml`: src holds only
 * content-addressable kinds ([[schema]]/test — uncollidable data hides from the fold), and the
 * offline contract must keep the EXACT bytes the live rail sent. Refresh by re-capturing.
 */
export const VIES_WSDL_XML = "<?xml version=\"1.0\"?><wsdl:definitions xmlns:wsdl=\"http://schemas.xmlsoap.org/wsdl/\">\n<wsdl:operation name=\"checkVat\">\n<wsdl:operation name=\"checkVatApprox\">\n<wsdl:operation name=\"checkVat\">\n<wsdl:operation name=\"checkVatApprox\">\n</wsdl:definitions>\n"
