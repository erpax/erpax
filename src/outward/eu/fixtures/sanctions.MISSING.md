HTTP 403 — the EU sanctions endpoint refuses a plain GET (observed 2026-08-19).
No fixture: we have never seen a body, so there is no contract to freeze. The outward
lane records this as `unreachable`, which is the honest state — not a passing test.
