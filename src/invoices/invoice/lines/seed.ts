import type { Payload } from 'payload'

/**
 * Seed for `invoice-lines` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedInvoiceLines(_payload: Payload): Promise<void> {
  // Opening data for `invoice-lines` is seeded here (none by default).
}
