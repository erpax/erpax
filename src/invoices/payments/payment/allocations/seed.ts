import type { Payload } from 'payload'

/**
 * Seed for `payment-allocations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPaymentAllocations(_payload: Payload): Promise<void> {
  // Opening data for `payment-allocations` is seeded here (none by default).
}
