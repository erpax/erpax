import type { Payload } from 'payload'

/**
 * Seed for `payments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPayments(_payload: Payload): Promise<void> {
  // Opening data for `payments` is seeded here (none by default).
}
