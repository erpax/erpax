import type { Payload } from 'payload'

/**
 * Seed for `customer-segments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCustomerSegments(_payload: Payload): Promise<void> {
  // Opening data for `customer-segments` is seeded here (none by default).
}
