<!-- GENERATED by src/readme/index.ts — agent deployment face; do NOT edit by hand.
     Run `pnpm readme` to regenerate; `pnpm computed:check` to verify (drift fails closed). -->

# collider

path `collider` · uuid `e6c1fdc9-b8b5-8ad9-a83a-77cd10dc6bef` · diamond `087f65c1-75ff-8546-ac7c-0cad39d1a90e`
horo `5` `round` · trinity `1·1·1` · sealed `1`
imports @/convention·@/cost · exports const·function·interface · escapes —
links `10/10` · folded `1` · bonds in `9` out `9`
faces worker·plugin·pwa `1·0·0`
neighbors → convention·cost·default·dry·exported·law·sealed·strength·tamper · ← convention·cost·default·dry·exported·law·sealed·strength·tamper
analysis —
law the collider has no default — it computes the corpus's tamper-cost purely from the convention-checks' coverages. Joint coverage is the product (every convention holds at once); tamper-cost is coverageCostLog2(joint, n). Zero entropy — every check computed clean — is ∞ by the math, not by assumption. No default-pass, no default-fail, no clamp, no fallback: only math.

---

<sub>content-uuid `35d4416a-3027-8639-b38e-c044841576b4` · `pnpm readme` · `pnpm computed:check`</sub>
