/**
 * SubscriptionPlans hook chains — placeholder.
 *
 * No collection-specific hooks are currently wired. The folder exists per
 * the canonical convention so per-plan lifecycle hooks (price-change
 * validation, billing-cycle invariants, deactivation cascade) have a
 * single import point when added.
 *
 * @accounting IFRS IFRS-15 revenue-from-contracts-with-customers
 * @accounting US-GAAP ASC-606 revenue-from-contracts-with-customers
 * @audit ISO-19011:2018 audit-trail
 * @see docs/STANDARDS.md §4.2
 */

export {}
