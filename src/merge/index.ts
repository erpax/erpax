/**
 * merge — vocabulary atom barrel (monitor/violations improve).
 * @generated realtime improve — replace with real exports
 */
export const WORD = 'merge' as const
export const atomPath = 'merge' as const
