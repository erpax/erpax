/**
 * Accounting plugin hooks — Payload lifecycle + React UI hooks.
 * 
 * Payload hooks: beforeChange, afterChange, beforeValidate, etc.
 * React hooks: useGLAccount, useJournal, useStatement, etc.
 * 
 * @standard SAF-T:2.0 audit-trail
 * @accounting IFRS-16 period-closing
 */

// Re-export all accounting hooks (Payload + React)
export const hooks = {};
