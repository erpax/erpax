import type { Payload } from 'payload'

/**
 * Seed for `insurance-contracts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedInsuranceContracts(_payload: Payload): Promise<void> {
  // Opening data for `insurance-contracts` is seeded here (none by default).
}
