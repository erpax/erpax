<!-- GENERATED by src/readme/index.ts — agent deployment face; do NOT edit by hand.
     Run `pnpm readme` to regenerate; `pnpm computed:check` to verify (drift fails closed). -->

# sealed

path `convention/sealed` · uuid `8873e75d-0265-8e47-a64e-d2a91e522591` · diamond `8c5db77c-65c8-8cd7-94b5-a577195ec59b`
horo `4` `weave` · trinity `1·1·1` · sealed `1`
imports — · exports function · escapes —
links `7/7` · folded `1` · bonds in `6` out `6`
faces worker·plugin·pwa `1·0·0`
neighbors → collider·convention·default·entropy·gate·law · ← collider·convention·default·entropy·gate·law
analysis —
law entropy leaves through error handling — a swallowed (empty catch) or defaulted (.catch(() => x)) error hides the truth, so the entropy escapes instead of propagating. The corpus is sealed only when errors propagate; coverage is the fraction of catches that do not leak (an upper bound — real leaks ≥ detected). It is the same sin as a defined default: an assumption that leaks entropy.

---

<sub>content-uuid `4b5f2e43-7242-84b1-9767-53a84b494b83` · `pnpm readme` · `pnpm computed:check`</sub>
