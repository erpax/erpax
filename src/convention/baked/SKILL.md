---
name: baked
description: "Use when reasoning about build-time projections of the corpus — any artifact fully derivable from src (the 71MB skills.index for the fs-less Cloudflare Worker, the docs tree) is gitignored and regenerated deterministically at build, never committed, because a stored derivable is duplicated state that can drift from its source; the tree is the one truth and each bundle a content-uuid projection of it, so a committed copy is stored entropy that lowers tamper-cost."
atomPath: "convention/baked"
coordinate: "convention/baked · 2/share · 50c8b550"
contentUuid: "c7cfcfd5-7709-5ec6-80c0-4506e443d191"
diamondUuid: "67cfcc96-53f7-8c6b-bcef-13f44026fdb5"
uuid: "50c8b550-ac55-8eb1-a47a-e02b766ed22f"
horo: 2
typography:
  partition: convention
  bondDegree: 22
standards:
  - one source of truth — a committed derivable is duplicated state that can drift
bindings: []
signatures:
  computationUuid: "0bdb320d-a865-8637-a6b5-d7fa018d5d2e"
  stages:
    - stage: path
      stageUuid: "8db66e94-b952-8139-813f-5fbe931215b7"
    - stage: trinity
      stageUuid: "8016196e-f845-8bb3-8a4f-0fa8215e1256"
    - stage: boundary
      stageUuid: "0a7d6a18-a68e-85cf-8449-b310eed0b828"
    - stage: links
      stageUuid: "6b3b890c-f80b-806e-a646-3ef26c689261"
    - stage: horo
      stageUuid: "1c645814-8edd-8c85-8567-be8784686a67"
    - stage: seal
      stageUuid: "dad2f737-b056-8f49-9b06-f69749c11353"
    - stage: uuid
      stageUuid: "b0056a77-54d5-8128-9171-af6c7ff06482"
version: 2
---
# convention/baked — derivable artifacts are regenerated at build, never committed

THE CONVENTION: **any artifact fully derivable from `src/` is gitignored and regenerated deterministically at build — never committed.** A stored derivable is duplicated state: a second copy of the [[akashic]] source that can DRIFT from it. The tree is the one truth; each bundle is a content-uuid projection of it, so a committed copy is stored entropy that lowers tamper-[[cost]] (two sources, one forgeable without the other noticing).

Realised by the [[skill]]-router index: `src/skill/router/skills.index.ts` is the whole corpus baked into a module for the fs-less Cloudflare Worker (which has no runtime fs to read SKILL.md). A full build measured 68MB on 2026-07-16 — over GitHub's 50MB limit — while the file the repo carries is a CI stub with an empty pool. It is now gitignored and regenerated by `pnpm skill:index` before every `pnpm build`, so the repo carries the SOURCE, not the projection. The `docs/` tree is the same law — VitePress computes it from `src/` on demand; nothing under docs/ is committed.

The dual of [[fresh]] (the cache is never the source of truth): `fresh` forbids TRUSTING a derived copy; `baked` forbids COMMITTING one. The derived artifact may have to EXIST at runtime — so generate it at build, from the one source, and let git hold only the source.

Composes: [[fresh]] · [[skill]] · [[akashic]] · [[tamper]] · [[cost]] · [[law]].

**Law — [[law]]: any artifact fully derivable from src is gitignored and regenerated at build, never committed — a stored derivable is a second copy of the [[akashic]] source that can drift, stored entropy that lowers tamper-cost. The tree is the one truth; each bundle is a content-uuid projection regenerated deterministically.**

@audit derivable artifacts (skills.index, docs/) are gitignored — regenerated at build from src
@standard one source of truth — a committed derivable is duplicated state that can drift
