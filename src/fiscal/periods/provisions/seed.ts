import type { Payload } from 'payload'

/**
 * Seed for `provisions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedProvisions(_payload: Payload): Promise<void> {
  // Opening data for `provisions` is seeded here (none by default).
}
