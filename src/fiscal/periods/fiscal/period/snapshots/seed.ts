import type { Payload } from 'payload'

/**
 * Seed for `fiscal-period-snapshots` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedFiscalPeriodSnapshots(_payload: Payload): Promise<void> {
  // Opening data for `fiscal-period-snapshots` is seeded here (none by default).
}
