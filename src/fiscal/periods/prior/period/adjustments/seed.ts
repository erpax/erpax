import type { Payload } from 'payload'

/**
 * Seed for `prior-period-adjustments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPriorPeriodAdjustments(_payload: Payload): Promise<void> {
  // Opening data for `prior-period-adjustments` is seeded here (none by default).
}
