import type { Payload } from 'payload'

/**
 * Seed for `post-balance-sheet-events` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPostBalanceSheetEvents(_payload: Payload): Promise<void> {
  // Opening data for `post-balance-sheet-events` is seeded here (none by default).
}
