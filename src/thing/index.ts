/**
 * thing — vocabulary atom barrel (monitor/violations improve).
 * @generated realtime improve — replace with real exports
 */
export const WORD = 'thing' as const
export const atomPath = 'thing' as const
