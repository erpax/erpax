/**
 * medical/estimator — medical facet; vocabulary pivot to @/estimator.
 */
export const PART = 'estimator' as const
export const CANONICAL = 'estimator' as const
export const PARENT = 'medical' as const
export const atomPath = 'medical/estimator' as const

/** Re-export stub — canonical vocabulary at @/estimator. */
export const reexportFrom = '@/estimator' as const

/** @index-cross.foldback child=medical/estimator parent=medical — this cross folds back into its parent. */
