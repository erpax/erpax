/**
 * medical/observational — medical facet; vocabulary pivot to @/observational.
 */
export const PART = 'observational' as const
export const CANONICAL = 'observational' as const
export const PARENT = 'medical' as const
export const atomPath = 'medical/observational' as const

/** Re-export stub — canonical vocabulary at @/observational. */
export const reexportFrom = '@/observational' as const
