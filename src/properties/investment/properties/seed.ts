import type { Payload } from 'payload'

/**
 * Seed for `investment-properties` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedInvestmentProperties(_payload: Payload): Promise<void> {
  // Opening data for `investment-properties` is seeded here (none by default).
}
