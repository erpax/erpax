import type { Payload } from 'payload'

/**
 * Seed for `properties` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedProperties(_payload: Payload): Promise<void> {
  // Opening data for `properties` is seeded here (none by default).
}
