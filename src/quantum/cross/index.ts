/**
 * quantum/cross — the quantum cross IS the fusion of a dual torus. This path re-exports
 * `@/dual/torus/fusion` verbatim: ONE identity, two names, folded to one canonical (the merge law — a
 * repeating pattern folded to itself, never written twice). The cross (⊕) is two tori fused into one.
 *
 * @see ../../dual/torus/fusion -- ./SKILL.md
 */
export * from '@/dual/torus/fusion'

/** @index-cross.foldback child=quantum/cross parent=quantum — this cross folds back into its parent. */
