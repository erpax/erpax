---
name: import
description: "Use when reasoning about quantum computed import — barrel entanglement derived from each file's live import/wikilink set, never hand-annotated; deep paths are escapes that collapse to @/x."
atomPath: "quantum/import"
coordinate: "quantum/import · 5/round · 144ac2fd"
contentUuid: "c4a0147b-8c39-51e0-b54d-c5484d048369"
diamondUuid: "0d79bfa6-5fef-89ba-ae68-45e142c98775"
uuid: "144ac2fd-1c22-8996-a392-95df30e78a8d"
horo: 5
typography:
  partition: quantum
  bondDegree: 42
standards:
  - the import graph is the config — barrels are the only public faces
bindings: []
signatures:
  computationUuid: "9057d8dc-2241-8f8e-88f3-80c92cd32998"
  stages:
    - stage: path
      stageUuid: "a3cf8055-9cf4-8ed4-8537-61238f0800fd"
    - stage: trinity
      stageUuid: "d30ba774-50aa-8d64-b9bb-ff361928db9c"
    - stage: boundary
      stageUuid: "9c8d80b6-fbb7-84ae-a8c7-1c4ac3a4e70a"
    - stage: links
      stageUuid: "1fecd6c9-6520-841a-b1be-2d88a5b3abf5"
    - stage: horo
      stageUuid: "21d311c3-e6cb-8789-a9d2-81587f52a368"
    - stage: seal
      stageUuid: "71e7a4b7-c645-85ca-bdf0-400ffa8928a6"
    - stage: uuid
      stageUuid: "cce6f1dc-3689-8c2c-9266-70049daed062"
quantum:
  superposition:
    - cost
    - exported
    - fresh
    - import
    - law
    - lawful
    - method
    - quantum
    - tamper
    - superposition
  collapse:
    - "Use when reasoning about quantum computed import — barrel entanglement derived from each file's live import/wikilink set, never hand-annotated; deep paths are escapes that collapse to @/x."
    - "import set = parseTsImports / parseSkillImports — computed live"
    - "quantum computed import — entanglement with atom barrels only, derived from live source, never hand-drawn; deep imports are escapes that must collapse to `@/x`."
    - the import graph is the config — barrels are the only public faces
  seal:
    sandbox: false
    receipt: false
    pathFollow: true
    canonicalRecord: true
    analogResults: false
    speechResults: false
    computationUuid: "9057d8dc-2241-8f8e-88f3-80c92cd32998"
    contentUuid: "c4a0147b-8c39-51e0-b54d-c5484d048369"
version: 2
---
# quantum/import — quantum **computed** import

The quantum twin of [[import]] ([[convention]]/import · [[tamper]]/import): **import is entanglement with another diamond's public face** — the atom barrel `@/x` — never deep-file penetration ([[convention]]/shallow).

**Computed, never authored:** for each TS file, `parseTsImports` derives the `@/` spec set; index-only specs are barrel entanglements; deep specs are escapes. For each SKILL.md, `[[wikilink]]` extraction ([[aura]]) derives outgoing entanglements. No manual `// @quantum-import` annotations — the boundary organ reads bytes.

Mass rewrite serves computation: collapsing deep imports → barrels purifies the computed import graph and raises [[tamper]]-cost (coverage → 1 ⇒ ∞).

Composes [[quantum/boundary]] · [[convention]]/import · [[tamper]]/import · [[entanglement]] · [[cost]].

**Law — [[law]]: quantum computed import — entanglement with atom barrels only, derived from live source, never hand-drawn; deep imports are escapes that must collapse to `@/x`.**

@audit import set = parseTsImports / parseSkillImports — computed live
@standard the import graph is the config — barrels are the only public faces

<sub>content-uuid `c4a0147b-8c39-51e0-b54d-c5484d048369` · account `quantum/import` · `pnpm skill:upgrade` · `pnpm computed:check`</sub>
