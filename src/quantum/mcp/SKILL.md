---
name: quantum-mcp
description: Use when reasoning about the MCP gateway in the quantum frame — one door exposing the whole trinity (payload data tools plus vitepress skill tools), the surface generated all-at-once from the corpus, every call by the actor-merge agent access-gated, quantum-compressed, and receipt-logged from the inside.
---

# quantum/mcp — the trinity as one tool surface

The quantum twin of [[mcp]]: the gateway serves the **whole trinity** through one door — **[[payload]]** (every collection as find/create/update/delete tools) ⊕ **[[vitepress]]** (the [[skills|SKILL.md]] corpus as readable, invokable skills). Matter and antimatter, data and capability, one endpoint.

The surface is a **projection of the corpus**, generated all-at-once ([[quantum/generator]]) — add an [[atom]] or a skill and the tools manifest with no hand-list, no drift. The [[skills]] router is the antimatter half: any path resolves against the SKILL.md corpus, served in the requested format.

**Use is trust-native, from the inside.** An [[agent]] is a human actor moving infinitely faster (the actor-merge), so the door opens onto everything a human can reach — but every call passes the gate: [[access]] inherits the key owner's reach, the [[sandbox]] checks capability, the [[broker]] holds the credential, the act emits a [[receipt]] (the uuid-chained [[quantum/log]] — each call adds tamper-cost). Calls are [[quantum/request]]-compressed (content-uuid = idempotency, cache = $0). It is an external trust-wrapper done from the inside — power and proof in one move. [[mcp]] ⊕ quantum/mcp are the two coils — the door and the discipline.

@see [[mcp]] · [[payload]] · [[vitepress]] · [[skills]] · [[agent]] · [[access]] · [[sandbox]] · [[receipt]] · [[quantum/log]] · [[quantum/request]]
