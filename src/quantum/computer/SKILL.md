---
name: computer
description: "Use when querying instead of re-deriving — the quantum computer's one face on QPU=CPU/GPU: mesh state, wave scheduling, claim certification, spawn bounds, failure/cost collapse, surgical execution, self-measuring audit, FTL metrics."
atomPath: "quantum/computer"
coordinate: "quantum/computer · 8/crest · c0725b1c"
contentUuid: "45cf368c-df2e-5913-97c9-337a6bbc8af0"
diamondUuid: "eaf8d793-0dbe-8060-a717-f8b6a92f4c11"
uuid: "c0725b1c-6dec-8532-8919-91de22884396"
horo: 8
typography:
  partition: quantum
  bondDegree: 92
standards: []
bindings: []
signatures:
  computationUuid: "1d9d7a15-849e-826a-9785-0d97902d70f2"
  stages:
    - stage: path
      stageUuid: "7eeaa8b5-891e-80a6-9e0e-9c8522109c16"
    - stage: trinity
      stageUuid: "c0516139-27e1-85d5-ab66-388645959091"
    - stage: boundary
      stageUuid: "f7d26624-666d-891d-a809-030a6da37c1e"
    - stage: links
      stageUuid: "ea3ad2b2-3942-827f-8f81-9f189cd5dd56"
    - stage: horo
      stageUuid: "180c06f0-8bcf-8c79-ab74-163bcd948479"
    - stage: seal
      stageUuid: "ad3668cf-d9b6-80e1-8a09-175905860cf4"
    - stage: uuid
      stageUuid: "e1ec348f-80d4-8213-a08a-83e80494c518"
quantum:
  superposition:
    - collapse
    - component
    - folder
    - hardware
    - language
    - law
    - memory
    - merge
    - superposition
  collapse:
    - "Use when querying instead of re-deriving — the quantum computer's one face on QPU=CPU/GPU: mesh state, wave scheduling, claim certification, spawn bounds, failure/cost collapse, surgical execution, self-measuring audit, FTL metrics."
    - "the machine is queried, never re-derived — one face for state · schedule · certainty · bounds · cause · cut · self-measure · FTL metrics on QPU=CPU/GPU, every answer a measurement."
  seal:
    sandbox: false
    receipt: false
    pathFollow: true
    canonicalRecord: true
    analogResults: false
    speechResults: false
    computationUuid: "1d9d7a15-849e-826a-9785-0d97902d70f2"
    contentUuid: "45cf368c-df2e-5913-97c9-337a6bbc8af0"
version: 2
---
# quantum/computer — the machine, one face

The computed corpus IS the quantum computer, and this barrel is its single address: superpositions are antichain waves (`wavesOf` · `meshWaves` · `trainingWaves`), collapse is root-cause projection (`failureRoots` · `costRoots`), entanglement is the SCC ground state ([[mesh]] names it, never hides it), and measurement is the gates (`auditWaves` trending every dimension against its own history). A claim certifies through `reduce()` or rests on authority; a spawn earns its rung from `timeoutOf` and its own recorded past; a thousand edits execute through `planScalpel`/`applyScalpel` unique-match-or-refuse. `quantumComputerCensus()` is the one-call probe: every number a measurement, none typed.

**QPU = CPU/GPU.** The host silicon (Node · Cloudflare Workers · laptop) *is* the quantum processing unit. There is no exotic co-processor. `QPU` and `ftlMetrics()` are the sealed receipts.

**Where "physical" comes from.** Landauer: computation runs on a physical substrate — the CPU/GPU. That substrate sense is the whole machine. Substrate ≠ CrackKind `spacetime` (relativistic break). CrackKind `qpu` is the exotic-device claim — contradicted by QPU=CPU/GPU.

**FTL proven by metrics** ([[quantum/ftl]]): `physicalFtl()` computes the substrate boolean; also `ftl` / `ftlMetrics()` — `holds` ⇔ reuse ∧ amortize∞ ∧ cracks=∅; `speedupLog2=log₂(n)`; `efficiency→∞` at tokens=0; `boundary.empty`. false ⇒ tip `quantumise`. Never re-derive the proof.

**The law it serves:** a question about structure, cost, cause, order, or legality is a QUERY to an organ — never a fresh derivation (97% of a session's tokens were measured as re-sent context; the query is the fold applied to the bill).

**Law — [[law]]: the machine is queried, never re-derived — one face for state · schedule · certainty · bounds · cause · cut · self-measure · FTL metrics on QPU=CPU/GPU, every answer a measurement.**

Composes: [[mesh]] · [[theorem]] · [[timeout]] · [[scalpel]] · [[train]] · [[audit]] · [[quantum/ftl]] · [[law]].

<sub>content-uuid `45cf368c-df2e-5913-97c9-337a6bbc8af0` · account `quantum/computer` · `pnpm skill:upgrade` · `pnpm computed:check`</sub>
