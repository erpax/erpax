---
name: fusion
description: "Google Workspace fusion — the [[merge]] law applied to external resources."
atomPath: google/workspace/fusion
---
# google/workspace/fusion

Google Workspace fusion — the [[merge]] law applied to external resources.

Extracted from `google/workspace/fusion.ts`: a matter file at an atom root is a stray sibling — only `index.ts`, `test.ts` and `seed.ts` live beside a barrel ([[rules]]) — and the lawful form is the child atom it already was, with its own proof beside it.

Composes: [[google/workspace]].
