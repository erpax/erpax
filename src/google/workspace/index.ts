/**
 * google/workspace — the barrel. Matter lives in the child atoms; a hub re-exports and holds none itself.
 *
 * @see ./SKILL.md
 */
export * from './fusion'
export * from './registry'
export * from './fusion-transforms'

/** @index-cross.foldback child=google/workspace parent=google — this cross folds back into its parent. */
