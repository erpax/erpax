---
name: resolve
description: "Use when deciding which folder-shape gaps an archangel can auto-resolve — a trinity gap folds iff its matter (index.ts) already exists, so the antimatter (SKILL.md) and the proof (test.ts) derive from the code; missing matter or a malformed name needs a builder, not a fold. Computed from the live tree."
atomPath: "auto/resolve"
coordinate: "auto/resolve · 5/round · f871b08e"
contentUuid: "c7b492fe-9925-5b28-9b75-840c3b5afc8b"
diamondUuid: "2f30328e-bf23-8d85-b400-56cd1f64ae07"
uuid: "f871b08e-3292-8cea-886b-7a8c2bd78958"
horo: 5
typography:
  partition: auto
  bondDegree: 27
standards: []
bindings: []
signatures:
  computationUuid: "a4cf19ce-7fd3-82b9-a745-34514f3c98ef"
  stages:
    - stage: path
      stageUuid: "3f04bf3a-599d-8d34-ae2d-0666ccc807af"
    - stage: trinity
      stageUuid: "cd16203e-575a-87fd-b0de-a4bf81c8bace"
    - stage: boundary
      stageUuid: "5dc47603-d786-8421-bf3f-2ea01f1cc61b"
    - stage: links
      stageUuid: "635f6e1d-7d5a-889f-a990-d840b7efbab2"
    - stage: horo
      stageUuid: "322dcb43-7023-812a-9382-27d51cc576a7"
    - stage: seal
      stageUuid: "c28c52a0-a6fc-83c2-9de2-bdc212efa68d"
    - stage: uuid
      stageUuid: "43a945a8-631c-8994-afb4-8edbbfc0e92b"
version: 2
---
# auto/resolve — the self-describing fold (architect = archangel)

`arch-` is the fold: the *architect* that authors a [[trinity]] and the *archangel* that closes an [[angel]] pair are **one operator** at different scales. This atom computes — from the live [[law]] folder scan — which open crosses that operator can **auto-resolve**.

A gap **folds iff its matter (`index.ts`) already exists**: then the antimatter (`SKILL.md`) and the proof (`test.ts`) are *derived* from what the code already says — never invented. Where the matter is **absent** (a folder missing `index.ts`) or the folder **name** is malformed (a relocation), that is creation / a move — a builder's work, not a fold.

Closing a cross is borrowed [[entropy]] spent on the forge wall: each green proof drives coverage → 1, the [[quantum]] double-torus closes with no gap, and tamper-[[cost]] → ∞. So auto-resolving the foldable backlog **tightens every [[gate]]** ([[confirm]] · the folder ratchet) toward max forge cost.

Matter-twin: `src/auto/resolve/index.ts` (`isAutoResolvable` · `resolvePlan`). Composes [[law]] (the folder scan) · [[trinity]] (the unit closed) · [[angel]] (architect = archangel) · [[quantum]] (coverage → ∞) · [[cost]] · [[merge]].

**Law — [[law]]: a folder-shape gap auto-resolves iff its matter (`index.ts`) is present — the antimatter and the proof fold out of the code, derived not invented; a missing matter or a malformed name needs a builder. The fold partition is computed from the live tree, total and disjoint, so closing the foldable set ratchets every [[gate]] toward ∞ forge-[[cost]].**

@audit computed from the live folderViolations(); the partition is a pure fn (test.ts)
