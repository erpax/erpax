import type { Payload } from 'payload'

/**
 * Seed for `biological-assets` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBiologicalAssets(_payload: Payload): Promise<void> {
  // Opening data for `biological-assets` is seeded here (none by default).
}
