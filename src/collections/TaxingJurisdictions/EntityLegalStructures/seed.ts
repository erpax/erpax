import type { Payload } from 'payload'

/**
 * Seed for `entity-legal-structures` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedEntityLegalStructures(_payload: Payload): Promise<void> {
  // Opening data for `entity-legal-structures` is seeded here (none by default).
}
