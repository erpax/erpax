import type { Payload } from 'payload'

/**
 * Seed for `statutory-field-mappings` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedStatutoryFieldMappings(_payload: Payload): Promise<void> {
  // Opening data for `statutory-field-mappings` is seeded here (none by default).
}
