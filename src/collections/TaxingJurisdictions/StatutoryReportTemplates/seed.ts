import type { Payload } from 'payload'

/**
 * Seed for `statutory-report-templates` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedStatutoryReportTemplates(_payload: Payload): Promise<void> {
  // Opening data for `statutory-report-templates` is seeded here (none by default).
}
