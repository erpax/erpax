import type { Payload } from 'payload'

/**
 * Seed for `categories` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCategories(_payload: Payload): Promise<void> {
  // Opening data for `categories` is seeded here (none by default).
}
