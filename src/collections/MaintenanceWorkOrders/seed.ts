import type { Payload } from 'payload'

/**
 * Seed for `maintenance-work-orders` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedMaintenanceWorkOrders(_payload: Payload): Promise<void> {
  // Opening data for `maintenance-work-orders` is seeded here (none by default).
}
