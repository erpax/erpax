import type { Payload } from 'payload'

/**
 * Seed for `held-for-sale-classifications` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedHeldForSaleClassifications(_payload: Payload): Promise<void> {
  // Opening data for `held-for-sale-classifications` is seeded here (none by default).
}
