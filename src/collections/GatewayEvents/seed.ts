import type { Payload } from 'payload'

/**
 * Seed for `gateway-events` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedGatewayEvents(_payload: Payload): Promise<void> {
  // Opening data for `gateway-events` is seeded here (none by default).
}
