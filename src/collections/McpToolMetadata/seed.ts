import type { Payload } from 'payload'

/**
 * Seed for `mcp-tool-metadata` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedMcpToolMetadata(_payload: Payload): Promise<void> {
  // Opening data for `mcp-tool-metadata` is seeded here (none by default).
}
