import type { Payload } from 'payload'

/**
 * Seed for `financial-statements` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedFinancialStatements(_payload: Payload): Promise<void> {
  // Opening data for `financial-statements` is seeded here (none by default).
}
