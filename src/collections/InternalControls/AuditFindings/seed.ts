import type { Payload } from 'payload'

/**
 * Seed for `audit-findings` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAuditFindings(_payload: Payload): Promise<void> {
  // Opening data for `audit-findings` is seeded here (none by default).
}
