import type { Payload } from 'payload'

/**
 * Seed for `internal-controls` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedInternalControls(_payload: Payload): Promise<void> {
  // Opening data for `internal-controls` is seeded here (none by default).
}
