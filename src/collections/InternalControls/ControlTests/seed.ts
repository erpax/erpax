import type { Payload } from 'payload'

/**
 * Seed for `control-tests` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedControlTests(_payload: Payload): Promise<void> {
  // Opening data for `control-tests` is seeded here (none by default).
}
