import type { Payload } from 'payload'

/**
 * Seed for `journal-entries` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedJournalEntries(_payload: Payload): Promise<void> {
  // Opening data for `journal-entries` is seeded here (none by default).
}
