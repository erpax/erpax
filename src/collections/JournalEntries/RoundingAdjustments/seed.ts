import type { Payload } from 'payload'

/**
 * Seed for `rounding-adjustments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRoundingAdjustments(_payload: Payload): Promise<void> {
  // Opening data for `rounding-adjustments` is seeded here (none by default).
}
