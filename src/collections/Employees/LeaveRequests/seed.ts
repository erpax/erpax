import type { Payload } from 'payload'

/**
 * Seed for `leave-requests` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedLeaveRequests(_payload: Payload): Promise<void> {
  // Opening data for `leave-requests` is seeded here (none by default).
}
