import type { Payload } from 'payload'

/**
 * Seed for `sales-commissions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedSalesCommissions(_payload: Payload): Promise<void> {
  // Opening data for `sales-commissions` is seeded here (none by default).
}
