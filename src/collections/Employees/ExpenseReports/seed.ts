import type { Payload } from 'payload'

/**
 * Seed for `expense-reports` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedExpenseReports(_payload: Payload): Promise<void> {
  // Opening data for `expense-reports` is seeded here (none by default).
}
