import type { Payload } from 'payload'

/**
 * Seed for `employees` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedEmployees(_payload: Payload): Promise<void> {
  // Opening data for `employees` is seeded here (none by default).
}
