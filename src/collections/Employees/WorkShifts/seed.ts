import type { Payload } from 'payload'

/**
 * Seed for `work-shifts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedWorkShifts(_payload: Payload): Promise<void> {
  // Opening data for `work-shifts` is seeded here (none by default).
}
