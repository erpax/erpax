import type { Payload } from 'payload'

/**
 * Seed for `lease-modifications` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedLeaseModifications(_payload: Payload): Promise<void> {
  // Opening data for `lease-modifications` is seeded here (none by default).
}
