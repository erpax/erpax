import type { Payload } from 'payload'

/**
 * Seed for `leases` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedLeases(_payload: Payload): Promise<void> {
  // Opening data for `leases` is seeded here (none by default).
}
