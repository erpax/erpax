import type { Payload } from 'payload'

/**
 * Seed for `lease-period-postings` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedLeasePeriodPostings(_payload: Payload): Promise<void> {
  // Opening data for `lease-period-postings` is seeded here (none by default).
}
