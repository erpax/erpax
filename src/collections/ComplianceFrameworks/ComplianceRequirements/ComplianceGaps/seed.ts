import type { Payload } from 'payload'

/**
 * Seed for `compliance-gaps` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedComplianceGaps(_payload: Payload): Promise<void> {
  // Opening data for `compliance-gaps` is seeded here (none by default).
}
