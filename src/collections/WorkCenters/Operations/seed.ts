import type { Payload } from 'payload'

/**
 * Seed for `operations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedOperations(_payload: Payload): Promise<void> {
  // Opening data for `operations` is seeded here (none by default).
}
