import type { Payload } from 'payload'

/**
 * Seed for `work-centers` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedWorkCenters(_payload: Payload): Promise<void> {
  // Opening data for `work-centers` is seeded here (none by default).
}
