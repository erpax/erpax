import type { Payload } from 'payload'

/**
 * Seed for `evidence-attestations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedEvidenceAttestations(_payload: Payload): Promise<void> {
  // Opening data for `evidence-attestations` is seeded here (none by default).
}
