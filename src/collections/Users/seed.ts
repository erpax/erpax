import type { Payload } from 'payload'

/**
 * Seed for `users` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedUsers(_payload: Payload): Promise<void> {
  // Opening data for `users` is seeded here (none by default).
}
