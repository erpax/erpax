import type { Payload } from 'payload'

/**
 * Seed for `maintenance-requests` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedMaintenanceRequests(_payload: Payload): Promise<void> {
  // Opening data for `maintenance-requests` is seeded here (none by default).
}
