import type { Payload } from 'payload'

/**
 * Seed for `mineral-resource-assets` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedMineralResourceAssets(_payload: Payload): Promise<void> {
  // Opening data for `mineral-resource-assets` is seeded here (none by default).
}
