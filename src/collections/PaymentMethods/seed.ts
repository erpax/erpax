import type { Payload } from 'payload'

/**
 * Seed for `payment-methods` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPaymentMethods(_payload: Payload): Promise<void> {
  // Opening data for `payment-methods` is seeded here (none by default).
}
