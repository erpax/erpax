import type { Payload } from 'payload'

/**
 * Seed for `audit-evidence` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAuditEvidence(_payload: Payload): Promise<void> {
  // Opening data for `audit-evidence` is seeded here (none by default).
}
