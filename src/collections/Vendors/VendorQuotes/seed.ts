import type { Payload } from 'payload'

/**
 * Seed for `vendor-quotes` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedVendorQuotes(_payload: Payload): Promise<void> {
  // Opening data for `vendor-quotes` is seeded here (none by default).
}
