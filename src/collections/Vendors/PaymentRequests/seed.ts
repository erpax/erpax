import type { Payload } from 'payload'

/**
 * Seed for `payment-requests` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPaymentRequests(_payload: Payload): Promise<void> {
  // Opening data for `payment-requests` is seeded here (none by default).
}
