import type { Payload } from 'payload'

/**
 * Seed for `vendor-scorecards` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedVendorScorecards(_payload: Payload): Promise<void> {
  // Opening data for `vendor-scorecards` is seeded here (none by default).
}
