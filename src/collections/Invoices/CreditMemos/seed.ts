import type { Payload } from 'payload'

/**
 * Seed for `credit-memos` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCreditMemos(_payload: Payload): Promise<void> {
  // Opening data for `credit-memos` is seeded here (none by default).
}
