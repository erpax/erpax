import type { Payload } from 'payload'

/**
 * Seed for `refunds` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRefunds(_payload: Payload): Promise<void> {
  // Opening data for `refunds` is seeded here (none by default).
}
