import type { Payload } from 'payload'

/**
 * Seed for `dunning-cycles` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedDunningCycles(_payload: Payload): Promise<void> {
  // Opening data for `dunning-cycles` is seeded here (none by default).
}
