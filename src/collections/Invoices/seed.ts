import type { Payload } from 'payload'

/**
 * Seed for `invoices` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedInvoices(_payload: Payload): Promise<void> {
  // Opening data for `invoices` is seeded here (none by default).
}
