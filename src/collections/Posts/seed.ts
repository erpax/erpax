import type { Payload } from 'payload'

/**
 * Seed for `posts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPosts(_payload: Payload): Promise<void> {
  // Opening data for `posts` is seeded here (none by default).
}
