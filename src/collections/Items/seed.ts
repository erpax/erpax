import type { Payload } from 'payload'

/**
 * Seed for `items` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedItems(_payload: Payload): Promise<void> {
  // Opening data for `items` is seeded here (none by default).
}
