import type { Payload } from 'payload'

/**
 * Seed for `inventory-movements` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedInventoryMovements(_payload: Payload): Promise<void> {
  // Opening data for `inventory-movements` is seeded here (none by default).
}
