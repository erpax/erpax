import type { Payload } from 'payload'

/**
 * Seed for `quality-inspections` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedQualityInspections(_payload: Payload): Promise<void> {
  // Opening data for `quality-inspections` is seeded here (none by default).
}
