import type { Payload } from 'payload'

/**
 * Seed for `goods-receipts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedGoodsReceipts(_payload: Payload): Promise<void> {
  // Opening data for `goods-receipts` is seeded here (none by default).
}
