import type { Payload } from 'payload'

/**
 * Seed for `operation-runs` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedOperationRuns(_payload: Payload): Promise<void> {
  // Opening data for `operation-runs` is seeded here (none by default).
}
