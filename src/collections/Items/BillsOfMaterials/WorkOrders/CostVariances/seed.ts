import type { Payload } from 'payload'

/**
 * Seed for `cost-variances` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCostVariances(_payload: Payload): Promise<void> {
  // Opening data for `cost-variances` is seeded here (none by default).
}
