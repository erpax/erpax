import type { Payload } from 'payload'

/**
 * Seed for `routings` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRoutings(_payload: Payload): Promise<void> {
  // Opening data for `routings` is seeded here (none by default).
}
