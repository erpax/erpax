import type { Payload } from 'payload'

/**
 * Seed for `bills-of-materials` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBillsOfMaterials(_payload: Payload): Promise<void> {
  // Opening data for `bills-of-materials` is seeded here (none by default).
}
