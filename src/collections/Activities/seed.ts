import type { Payload } from 'payload'

/**
 * Seed for `activities` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedActivities(_payload: Payload): Promise<void> {
  // Opening data for `activities` is seeded here (none by default).
}
