import type { Payload } from 'payload'

/**
 * Seed for `subscription-plans` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedSubscriptionPlans(_payload: Payload): Promise<void> {
  // Opening data for `subscription-plans` is seeded here (none by default).
}
