import type { Payload } from 'payload'

/**
 * Seed for `usage-records` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedUsageRecords(_payload: Payload): Promise<void> {
  // Opening data for `usage-records` is seeded here (none by default).
}
