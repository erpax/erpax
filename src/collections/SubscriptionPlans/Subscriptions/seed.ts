import type { Payload } from 'payload'

/**
 * Seed for `subscriptions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedSubscriptions(_payload: Payload): Promise<void> {
  // Opening data for `subscriptions` is seeded here (none by default).
}
