import type { Payload } from 'payload'

/**
 * Seed for `commitments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCommitments(_payload: Payload): Promise<void> {
  // Opening data for `commitments` is seeded here (none by default).
}
