import type { Payload } from 'payload'

/**
 * Seed for `contract-templates` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedContractTemplates(_payload: Payload): Promise<void> {
  // Opening data for `contract-templates` is seeded here (none by default).
}
