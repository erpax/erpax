import type { Payload } from 'payload'

/**
 * Seed for `standards` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedStandards(_payload: Payload): Promise<void> {
  // Opening data for `standards` is seeded here (none by default).
}
