import type { Payload } from 'payload'

/**
 * Seed for `financial-profiles` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedFinancialProfiles(_payload: Payload): Promise<void> {
  // Opening data for `financial-profiles` is seeded here (none by default).
}
