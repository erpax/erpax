import type { Payload } from 'payload'

/**
 * Seed for `terminals` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTerminals(_payload: Payload): Promise<void> {
  // Opening data for `terminals` is seeded here (none by default).
}
