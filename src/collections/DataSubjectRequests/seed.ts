import type { Payload } from 'payload'

/**
 * Seed for `data-subject-requests` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedDataSubjectRequests(_payload: Payload): Promise<void> {
  // Opening data for `data-subject-requests` is seeded here (none by default).
}
