import type { Payload } from 'payload'

/**
 * Seed for `api-audit-events` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedApiAuditEvents(_payload: Payload): Promise<void> {
  // Opening data for `api-audit-events` is seeded here (none by default).
}
