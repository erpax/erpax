import type { Payload } from 'payload'

/**
 * Seed for `translations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTranslations(_payload: Payload): Promise<void> {
  // Opening data for `translations` is seeded here (none by default).
}
