import type { Payload } from 'payload'

/**
 * Seed for `period-locks` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPeriodLocks(_payload: Payload): Promise<void> {
  // Opening data for `period-locks` is seeded here (none by default).
}
