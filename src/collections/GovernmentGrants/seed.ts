import type { Payload } from 'payload'

/**
 * Seed for `government-grants` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedGovernmentGrants(_payload: Payload): Promise<void> {
  // Opening data for `government-grants` is seeded here (none by default).
}
