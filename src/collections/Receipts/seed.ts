import type { Payload } from 'payload'

/**
 * Seed for `receipts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedReceipts(_payload: Payload): Promise<void> {
  // Opening data for `receipts` is seeded here (none by default).
}
