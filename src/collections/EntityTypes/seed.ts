import type { Payload } from 'payload'

/**
 * Seed for `entity-types` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedEntityTypes(_payload: Payload): Promise<void> {
  // Opening data for `entity-types` is seeded here (none by default).
}
