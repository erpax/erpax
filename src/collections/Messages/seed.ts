import type { Payload } from 'payload'

/**
 * Seed for `messages` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedMessages(_payload: Payload): Promise<void> {
  // Opening data for `messages` is seeded here (none by default).
}
