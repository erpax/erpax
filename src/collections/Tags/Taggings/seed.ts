import type { Payload } from 'payload'

/**
 * Seed for `taggings` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTaggings(_payload: Payload): Promise<void> {
  // Opening data for `taggings` is seeded here (none by default).
}
