import type { Payload } from 'payload'

/**
 * Seed for `purchase-requisitions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPurchaseRequisitions(_payload: Payload): Promise<void> {
  // Opening data for `purchase-requisitions` is seeded here (none by default).
}
