import type { Payload } from 'payload'

/**
 * Seed for `job-positions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedJobPositions(_payload: Payload): Promise<void> {
  // Opening data for `job-positions` is seeded here (none by default).
}
