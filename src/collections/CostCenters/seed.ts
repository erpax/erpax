import type { Payload } from 'payload'

/**
 * Seed for `cost-centers` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCostCenters(_payload: Payload): Promise<void> {
  // Opening data for `cost-centers` is seeded here (none by default).
}
