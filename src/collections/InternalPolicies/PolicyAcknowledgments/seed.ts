import type { Payload } from 'payload'

/**
 * Seed for `policy-acknowledgments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPolicyAcknowledgments(_payload: Payload): Promise<void> {
  // Opening data for `policy-acknowledgments` is seeded here (none by default).
}
