import type { Payload } from 'payload'

/**
 * Seed for `internal-policies` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedInternalPolicies(_payload: Payload): Promise<void> {
  // Opening data for `internal-policies` is seeded here (none by default).
}
