import type { Payload } from 'payload'

/**
 * Seed for `policy-versions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPolicyVersions(_payload: Payload): Promise<void> {
  // Opening data for `policy-versions` is seeded here (none by default).
}
