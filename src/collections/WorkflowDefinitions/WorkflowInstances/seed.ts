import type { Payload } from 'payload'

/**
 * Seed for `workflow-instances` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedWorkflowInstances(_payload: Payload): Promise<void> {
  // Opening data for `workflow-instances` is seeded here (none by default).
}
