import type { Payload } from 'payload'

/**
 * Seed for `currency-rates` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCurrencyRates(_payload: Payload): Promise<void> {
  // Opening data for `currency-rates` is seeded here (none by default).
}
