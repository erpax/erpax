import type { Payload } from 'payload'

/**
 * Seed for `account-reconciliations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAccountReconciliations(_payload: Payload): Promise<void> {
  // Opening data for `account-reconciliations` is seeded here (none by default).
}
