import type { Payload } from 'payload'

/**
 * Seed for `recurring-journals` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRecurringJournals(_payload: Payload): Promise<void> {
  // Opening data for `recurring-journals` is seeded here (none by default).
}
