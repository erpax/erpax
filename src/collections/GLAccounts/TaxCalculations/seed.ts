import type { Payload } from 'payload'

/**
 * Seed for `tax-calculations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTaxCalculations(_payload: Payload): Promise<void> {
  // Opening data for `tax-calculations` is seeded here (none by default).
}
