import type { Payload } from 'payload'

/**
 * Seed for `period-end-adjustments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPeriodEndAdjustments(_payload: Payload): Promise<void> {
  // Opening data for `period-end-adjustments` is seeded here (none by default).
}
