import type { Payload } from 'payload'

/**
 * Seed for `risk-register` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRiskRegister(_payload: Payload): Promise<void> {
  // Opening data for `risk-register` is seeded here (none by default).
}
