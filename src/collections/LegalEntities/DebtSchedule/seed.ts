import type { Payload } from 'payload'

/**
 * Seed for `debt-schedule` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedDebtSchedule(_payload: Payload): Promise<void> {
  // Opening data for `debt-schedule` is seeded here (none by default).
}
