import type { Payload } from 'payload'

/**
 * Seed for `compliance-notifications` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedComplianceNotifications(_payload: Payload): Promise<void> {
  // Opening data for `compliance-notifications` is seeded here (none by default).
}
