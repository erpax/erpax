import type { Payload } from 'payload'

/**
 * Seed for `audit-committees` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAuditCommittees(_payload: Payload): Promise<void> {
  // Opening data for `audit-committees` is seeded here (none by default).
}
