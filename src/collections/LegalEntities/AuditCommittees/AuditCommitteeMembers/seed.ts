import type { Payload } from 'payload'

/**
 * Seed for `audit-committee-members` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAuditCommitteeMembers(_payload: Payload): Promise<void> {
  // Opening data for `audit-committee-members` is seeded here (none by default).
}
