import type { Payload } from 'payload'

/**
 * Seed for `transfer-pricing-files` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTransferPricingFiles(_payload: Payload): Promise<void> {
  // Opening data for `transfer-pricing-files` is seeded here (none by default).
}
