import type { Payload } from 'payload'

/**
 * Seed for `intercompany-transactions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedIntercompanyTransactions(_payload: Payload): Promise<void> {
  // Opening data for `intercompany-transactions` is seeded here (none by default).
}
