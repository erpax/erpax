import type { Payload } from 'payload'

/**
 * Seed for `internal-audit-function` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedInternalAuditFunction(_payload: Payload): Promise<void> {
  // Opening data for `internal-audit-function` is seeded here (none by default).
}
