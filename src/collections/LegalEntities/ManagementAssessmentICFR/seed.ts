import type { Payload } from 'payload'

/**
 * Seed for `management-assessment-icfr` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedManagementAssessmentIcfr(_payload: Payload): Promise<void> {
  // Opening data for `management-assessment-icfr` is seeded here (none by default).
}
