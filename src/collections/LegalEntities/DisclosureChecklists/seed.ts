import type { Payload } from 'payload'

/**
 * Seed for `disclosure-checklists` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedDisclosureChecklists(_payload: Payload): Promise<void> {
  // Opening data for `disclosure-checklists` is seeded here (none by default).
}
