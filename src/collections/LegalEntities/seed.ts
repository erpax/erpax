import type { Payload } from 'payload'

/**
 * Seed for `legal-entities` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedLegalEntities(_payload: Payload): Promise<void> {
  // Opening data for `legal-entities` is seeded here (none by default).
}
