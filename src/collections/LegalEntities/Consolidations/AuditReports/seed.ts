import type { Payload } from 'payload'

/**
 * Seed for `audit-reports` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAuditReports(_payload: Payload): Promise<void> {
  // Opening data for `audit-reports` is seeded here (none by default).
}
