import type { Payload } from 'payload'

/**
 * Seed for `post-close-analytics-reports` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPostCloseAnalyticsReports(_payload: Payload): Promise<void> {
  // Opening data for `post-close-analytics-reports` is seeded here (none by default).
}
