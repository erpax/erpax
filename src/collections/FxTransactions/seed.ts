import type { Payload } from 'payload'

/**
 * Seed for `fx-transactions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedFxTransactions(_payload: Payload): Promise<void> {
  // Opening data for `fx-transactions` is seeded here (none by default).
}
