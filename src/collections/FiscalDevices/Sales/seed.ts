import type { Payload } from 'payload'

/**
 * Seed for `sales` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedSales(_payload: Payload): Promise<void> {
  // Opening data for `sales` is seeded here (none by default).
}
