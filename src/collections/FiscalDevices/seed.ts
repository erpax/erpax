import type { Payload } from 'payload'

/**
 * Seed for `fiscal-devices` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedFiscalDevices(_payload: Payload): Promise<void> {
  // Opening data for `fiscal-devices` is seeded here (none by default).
}
