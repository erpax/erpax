import type { Payload } from 'payload'

/**
 * Seed for `data-processing-activities` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedDataProcessingActivities(_payload: Payload): Promise<void> {
  // Opening data for `data-processing-activities` is seeded here (none by default).
}
