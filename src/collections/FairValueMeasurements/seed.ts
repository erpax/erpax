import type { Payload } from 'payload'

/**
 * Seed for `fair-value-measurements` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedFairValueMeasurements(_payload: Payload): Promise<void> {
  // Opening data for `fair-value-measurements` is seeded here (none by default).
}
