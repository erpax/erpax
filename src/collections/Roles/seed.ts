import type { Payload } from 'payload'

/**
 * Seed for `roles` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedRoles(_payload: Payload): Promise<void> {
  // Opening data for `roles` is seeded here (none by default).
}
