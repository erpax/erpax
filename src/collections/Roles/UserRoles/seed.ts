import type { Payload } from 'payload'

/**
 * Seed for `user-roles` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedUserRoles(_payload: Payload): Promise<void> {
  // Opening data for `user-roles` is seeded here (none by default).
}
