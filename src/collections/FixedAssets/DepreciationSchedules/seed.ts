import type { Payload } from 'payload'

/**
 * Seed for `depreciation-schedules` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedDepreciationSchedules(_payload: Payload): Promise<void> {
  // Opening data for `depreciation-schedules` is seeded here (none by default).
}
