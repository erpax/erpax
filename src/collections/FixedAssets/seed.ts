import type { Payload } from 'payload'

/**
 * Seed for `fixed-assets` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedFixedAssets(_payload: Payload): Promise<void> {
  // Opening data for `fixed-assets` is seeded here (none by default).
}
