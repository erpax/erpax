import type { Payload } from 'payload'

/**
 * Seed for `pages` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPages(_payload: Payload): Promise<void> {
  // Opening data for `pages` is seeded here (none by default).
}
