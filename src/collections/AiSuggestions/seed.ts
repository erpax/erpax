import type { Payload } from 'payload'

/**
 * Seed for `ai-suggestions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAiSuggestions(_payload: Payload): Promise<void> {
  // Opening data for `ai-suggestions` is seeded here (none by default).
}
