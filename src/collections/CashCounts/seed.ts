import type { Payload } from 'payload'

/**
 * Seed for `cash-counts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCashCounts(_payload: Payload): Promise<void> {
  // Opening data for `cash-counts` is seeded here (none by default).
}
