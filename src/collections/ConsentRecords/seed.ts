import type { Payload } from 'payload'

/**
 * Seed for `consent-records` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedConsentRecords(_payload: Payload): Promise<void> {
  // Opening data for `consent-records` is seeded here (none by default).
}
