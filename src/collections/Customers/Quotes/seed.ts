import type { Payload } from 'payload'

/**
 * Seed for `quotes` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedQuotes(_payload: Payload): Promise<void> {
  // Opening data for `quotes` is seeded here (none by default).
}
