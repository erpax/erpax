import type { Payload } from 'payload'

/**
 * Seed for `shipments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedShipments(_payload: Payload): Promise<void> {
  // Opening data for `shipments` is seeded here (none by default).
}
