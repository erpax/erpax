import type { Payload } from 'payload'

/**
 * Seed for `customs-declarations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCustomsDeclarations(_payload: Payload): Promise<void> {
  // Opening data for `customs-declarations` is seeded here (none by default).
}
