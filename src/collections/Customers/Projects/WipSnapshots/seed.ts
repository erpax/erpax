import type { Payload } from 'payload'

/**
 * Seed for `wip-snapshots` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedWipSnapshots(_payload: Payload): Promise<void> {
  // Opening data for `wip-snapshots` is seeded here (none by default).
}
