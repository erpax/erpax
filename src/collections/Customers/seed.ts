import type { Payload } from 'payload'

/**
 * Seed for `customers` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCustomers(_payload: Payload): Promise<void> {
  // Opening data for `customers` is seeded here (none by default).
}
