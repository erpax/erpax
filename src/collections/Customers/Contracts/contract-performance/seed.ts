import type { Payload } from 'payload'

/**
 * Seed for `contract-performance` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedContractPerformance(_payload: Payload): Promise<void> {
  // Opening data for `contract-performance` is seeded here (none by default).
}
