import type { Payload } from 'payload'

/**
 * Seed for `contract-amendments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedContractAmendments(_payload: Payload): Promise<void> {
  // Opening data for `contract-amendments` is seeded here (none by default).
}
