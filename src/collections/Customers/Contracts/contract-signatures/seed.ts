import type { Payload } from 'payload'

/**
 * Seed for `contract-signatures` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedContractSignatures(_payload: Payload): Promise<void> {
  // Opening data for `contract-signatures` is seeded here (none by default).
}
