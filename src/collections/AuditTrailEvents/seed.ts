import type { Payload } from 'payload'

/**
 * Seed for `audit-trail-events` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAuditTrailEvents(_payload: Payload): Promise<void> {
  // Opening data for `audit-trail-events` is seeded here (none by default).
}
