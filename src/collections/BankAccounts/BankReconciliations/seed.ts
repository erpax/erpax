import type { Payload } from 'payload'

/**
 * Seed for `bank-reconciliations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBankReconciliations(_payload: Payload): Promise<void> {
  // Opening data for `bank-reconciliations` is seeded here (none by default).
}
