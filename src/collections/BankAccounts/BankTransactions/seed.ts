import type { Payload } from 'payload'

/**
 * Seed for `bank-transactions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBankTransactions(_payload: Payload): Promise<void> {
  // Opening data for `bank-transactions` is seeded here (none by default).
}
