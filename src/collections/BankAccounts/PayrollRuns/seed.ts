import type { Payload } from 'payload'

/**
 * Seed for `payroll-runs` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedPayrollRuns(_payload: Payload): Promise<void> {
  // Opening data for `payroll-runs` is seeded here (none by default).
}
