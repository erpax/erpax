import type { Payload } from 'payload'

/**
 * Seed for `bank-accounts` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBankAccounts(_payload: Payload): Promise<void> {
  // Opening data for `bank-accounts` is seeded here (none by default).
}
