import type { Payload } from 'payload'

/**
 * Seed for `ai-models` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAiModels(_payload: Payload): Promise<void> {
  // Opening data for `ai-models` is seeded here (none by default).
}
