import type { Payload } from 'payload'

/**
 * Seed for `transaction-failures` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTransactionFailures(_payload: Payload): Promise<void> {
  // Opening data for `transaction-failures` is seeded here (none by default).
}
