import type { Payload } from 'payload'

/**
 * Seed for `audit-events` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedAuditEvents(_payload: Payload): Promise<void> {
  // Opening data for `audit-events` is seeded here (none by default).
}
