import type { Payload } from 'payload'

/**
 * Seed for `budget-planning` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedBudgetPlanning(_payload: Payload): Promise<void> {
  // Opening data for `budget-planning` is seeded here (none by default).
}
