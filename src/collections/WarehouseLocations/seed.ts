import type { Payload } from 'payload'

/**
 * Seed for `warehouse-locations` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedWarehouseLocations(_payload: Payload): Promise<void> {
  // Opening data for `warehouse-locations` is seeded here (none by default).
}
