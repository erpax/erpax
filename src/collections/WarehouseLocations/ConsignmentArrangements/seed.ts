import type { Payload } from 'payload'

/**
 * Seed for `consignment-arrangements` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedConsignmentArrangements(_payload: Payload): Promise<void> {
  // Opening data for `consignment-arrangements` is seeded here (none by default).
}
