import type { Payload } from 'payload'

/**
 * Seed for `consignment-sales` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedConsignmentSales(_payload: Payload): Promise<void> {
  // Opening data for `consignment-sales` is seeded here (none by default).
}
