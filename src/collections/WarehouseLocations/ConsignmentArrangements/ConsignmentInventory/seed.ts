import type { Payload } from 'payload'

/**
 * Seed for `consignment-inventory` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedConsignmentInventory(_payload: Payload): Promise<void> {
  // Opening data for `consignment-inventory` is seeded here (none by default).
}
