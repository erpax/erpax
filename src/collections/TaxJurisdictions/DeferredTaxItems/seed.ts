import type { Payload } from 'payload'

/**
 * Seed for `deferred-tax-items` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedDeferredTaxItems(_payload: Payload): Promise<void> {
  // Opening data for `deferred-tax-items` is seeded here (none by default).
}
