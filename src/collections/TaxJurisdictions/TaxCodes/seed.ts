import type { Payload } from 'payload'

/**
 * Seed for `tax-codes` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTaxCodes(_payload: Payload): Promise<void> {
  // Opening data for `tax-codes` is seeded here (none by default).
}
