import type { Payload } from 'payload'

/**
 * Seed for `tax-returns` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTaxReturns(_payload: Payload): Promise<void> {
  // Opening data for `tax-returns` is seeded here (none by default).
}
