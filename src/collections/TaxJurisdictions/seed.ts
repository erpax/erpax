import type { Payload } from 'payload'

/**
 * Seed for `tax-jurisdictions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTaxJurisdictions(_payload: Payload): Promise<void> {
  // Opening data for `tax-jurisdictions` is seeded here (none by default).
}
