import type { Payload } from 'payload'

/**
 * Seed for `operators` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedOperators(_payload: Payload): Promise<void> {
  // Opening data for `operators` is seeded here (none by default).
}
