import type { Payload } from 'payload'

/**
 * Seed for `csrd-disclosures` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCsrdDisclosures(_payload: Payload): Promise<void> {
  // Opening data for `csrd-disclosures` is seeded here (none by default).
}
