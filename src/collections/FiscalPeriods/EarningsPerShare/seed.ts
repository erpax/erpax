import type { Payload } from 'payload'

/**
 * Seed for `earnings-per-share` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedEarningsPerShare(_payload: Payload): Promise<void> {
  // Opening data for `earnings-per-share` is seeded here (none by default).
}
