import type { Payload } from 'payload'

/**
 * Seed for `carbon-emissions` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedCarbonEmissions(_payload: Payload): Promise<void> {
  // Opening data for `carbon-emissions` is seeded here (none by default).
}
