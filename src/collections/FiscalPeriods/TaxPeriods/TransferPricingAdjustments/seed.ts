import type { Payload } from 'payload'

/**
 * Seed for `transfer-pricing-adjustments` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTransferPricingAdjustments(_payload: Payload): Promise<void> {
  // Opening data for `transfer-pricing-adjustments` is seeded here (none by default).
}
