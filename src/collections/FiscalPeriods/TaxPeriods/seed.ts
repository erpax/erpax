import type { Payload } from 'payload'

/**
 * Seed for `tax-periods` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTaxPeriods(_payload: Payload): Promise<void> {
  // Opening data for `tax-periods` is seeded here (none by default).
}
