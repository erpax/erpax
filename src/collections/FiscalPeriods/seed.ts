import type { Payload } from 'payload'

/**
 * Seed for `fiscal-periods` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedFiscalPeriods(_payload: Payload): Promise<void> {
  // Opening data for `fiscal-periods` is seeded here (none by default).
}
