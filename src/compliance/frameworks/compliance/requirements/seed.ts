import type { Payload } from 'payload'

/**
 * Seed for `compliance-requirements` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedComplianceRequirements(_payload: Payload): Promise<void> {
  // Opening data for `compliance-requirements` is seeded here (none by default).
}
