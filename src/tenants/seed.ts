import type { Payload } from 'payload'

/**
 * Seed for `tenants` — co-located unified-node opening data.
 * @see ./index.ts
 */
export async function seedTenants(_payload: Payload): Promise<void> {
  // Opening data for `tenants` is seeded here (none by default).
}
